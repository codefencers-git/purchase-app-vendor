import 'package:flutter/material.dart';
import 'package:purchase_vendor/widgets/app_text.dart';

class NewDesignScreen extends StatelessWidget {
  const NewDesignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: AppText(
        text: "Login",
      ),
    );
  }
}
